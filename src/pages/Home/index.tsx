import React from "react";
import {Container, Content} from './styles';
import Header from '../../components/Header'

const Home: React.FC = () => {
  return (
      <Header/>
    );
};
export default Home;
